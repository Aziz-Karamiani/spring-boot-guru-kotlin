spring:
  application:
    name: "Spring Boot With kotlin(Guru)"
  datasource:
    url: jdbc:h2:mem:guru-kotlin
    driver-class-name: org.h2.Driver
    username: root
    password: password
  jpa:
    database: h2
    database-platform: org.hibernate.dialect.H2Dialect
    generate-ddl: true
    show-sql: true
  h2:
    console:
      enabled: true